version: 2
project_name: postq

before:
  hooks:
    - go mod tidy

builds:
  - id: postq
    main: ./cmd/postq
    binary: postq
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
      - -X main.commit={{ .Commit }}
      - -X main.date={{ .Date }}
  - id: postq-mcp
    main: ./cmd/postq-mcp
    binary: postq-mcp
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin, windows]
    goarch: [amd64, arm64]
    ldflags:
      - -s -w
      - -X main.serverVersion={{ .Version }}

archives:
  - id: postq
    ids: [postq, postq-mcp]
    formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: "checksums.txt"

sboms:
  - artifacts: archive

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^ci:"
      - "^test:"
  groups:
    - title: Features
      regexp: "^.*feat[(\\w)]*:+.*$"
      order: 0
    - title: Bug Fixes
      regexp: "^.*fix[(\\w)]*:+.*$"
      order: 1
    - title: Other
      order: 999

brews:
  - name: postq
    repository:
      owner: PostQDev
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://postq.dev"
    description: "PostQ CLI and MCP server for quantum-risk scanning and hybrid signing."
    license: "MIT"
    install: |
      bin.install "postq"
      bin.install "postq-mcp"
    test: |
      system "#{bin}/postq", "version"
      assert_match "postq-mcp", shell_output("#{bin}/postq-mcp </dev/null 2>&1")

release:
  github:
    owner: PostQDev
    name: postq-cli
  draft: false
  prerelease: auto
  name_template: "v{{ .Version }}"
  header: |
    ## PostQ CLI v{{ .Version }}

    Install via Homebrew:
    ```
    brew install PostQDev/tap/postq
    ```

    Or grab a pre-built binary below.

    Each archive also contains `postq-mcp`, the bounded stdio MCP server for
    `scan_url`, `scan_code`, `sign`, and `verify` agent integrations.
