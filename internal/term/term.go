// Package term provides minimal terminal helpers (size, resize signal,
// raw-mode input) using only the Go standard library. Works on darwin &
// linux; Windows falls back to sensible defaults / no-op raw mode.
package term
